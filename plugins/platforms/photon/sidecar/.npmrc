# The postinstall patch edits files under node_modules/@spectrum-ts/imessage,
# a transitive dependency: keep a flat tree so that path exists, and copy
# files instead of hard-linking them so the patch never reaches the pnpm store.
node-linker=hoisted
package-import-method=copy
